// tests for functions in lib
// test that they return a callback function
//correct value when passed valid parameters, and that they return a predictable value (and don't crash) when passed invalid parameters.
